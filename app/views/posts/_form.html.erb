<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2 class="text-warning"><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><p class="text-warning"><%= msg %></p></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field" >
    <%= f.label :body %><br>
    <%= f.text_area :body %><br />
  </div>
  <div class="field"><br />
    <%= f.submit class: 'btn btn-primary' %><br />
  </div>
<% end %>
